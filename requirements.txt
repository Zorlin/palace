# Palace dependencies

# Anthropic SDK (for model ranking and API calls)
anthropic>=0.75.0

# HTTP requests (for API calls and model ranking)
requests>=2.31.0

# MCP Integration (REQUIRED for permission handling)
mcp>=0.1.0

# Streaming JSON output parsing
json-stream>=2.0.0

# Interactive menus (BG3 style action selection)
questionary>=2.0.0

# Testing (Golden Rule #1: TDD)
pytest>=7.4.0
pytest-cov>=4.1.0
